pipeline {
  agent any 
  tools {
    maven 'Maven'
  }
  
  stages {
    stage('Initialize') {
      steps {
        sh '''
          echo "PATH = ${PATH}"
          echo "M2_HOME = ${M2_HOME}"
          '''
      }
    }
    stage('Build') {
      steps {
        
          sh 'mvn clean package'
        
      }
    }
    stage ('Deploy-To-Tomcat') {
            steps {
           sshagent(['da94cba3-7e62-4216-ba37-e2e43005f320']) {
                sh 'scp -o StrictHostKeyChecking=no target/*.war ubuntu@16.170.247.79:/prod/apache-tomcat-9.0.79/webapps/webapp.war'
              }      
           }       
    }
  }
}
